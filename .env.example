# football-data.org free-tier API key
# Sign up at https://www.football-data.org/client/register
# Then set VITE_FOOTBALL_DATA_KEY in Vercel project settings (or copy this
# file to .env.local for local development — .env.local is gitignored).
VITE_FOOTBALL_DATA_KEY=your_key_here
